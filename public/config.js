// API base URL — leave empty for local dev (same-origin Express server).
// Set to your deployed Cloudflare Worker URL for production GitHub Pages hosting.
// Example: const CONFIG = { API_URL: 'https://floco-permits.YOUR_SUBDOMAIN.workers.dev' };
const CONFIG = { API_URL: '' };
